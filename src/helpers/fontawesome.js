/** FontAwesomeIcons */
import {
  library
} from '@fortawesome/fontawesome-svg-core';

import {
  faColumns,
  faTrash,
} from '@fortawesome/free-solid-svg-icons';

library.add(
  faColumns,
  faTrash,
);
